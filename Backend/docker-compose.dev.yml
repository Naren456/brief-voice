# Run just the Qdrant dependency for local backend development.
# Usage (from Backend/): docker compose -f docker-compose.dev.yml up -d
services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
    volumes:
      - ../qdrant_storage:/qdrant/storage

volumes: {}
